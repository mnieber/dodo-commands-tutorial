DOCKER:
  enabled: true
  image: dodo_tutorial:1604
  options:
    '*':
      image: ${/DOCKER/image}
      volume_map:
        ${/ROOT/project_dir}/log: /var/log

ROOT:
  decorators:
    docker: [cmake, docker]
