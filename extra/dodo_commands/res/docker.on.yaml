DOCKER:
  images:
    base:
      image: dodo_tutorial:1604
      build_dir: ${/ROOT/res_dir}
  options:
    '*':
      image: ${/DOCKER/images/base/image}
      volume_map:
        ${/ROOT/project_dir}/log: /var/log

ROOT:
  decorators:
    docker: [cmake, docker]
